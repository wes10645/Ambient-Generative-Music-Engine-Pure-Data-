## Pure Data Patch

The main DSP implementation can be found in:

`patches/ambient_engine.pd`

To run:
1. Open Pure Data (Pd)
2. Open the patch file
3. Enable DSP
4. Press the trigger key (Q) to start playback
